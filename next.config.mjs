/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			'sammi.ac',
			'utfs.io',
			'blog.sammi.app',
			'static.independent.co.uk',
		],
	},
};

export default nextConfig;
